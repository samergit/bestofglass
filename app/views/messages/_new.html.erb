	<div class="msgtextarea"> 
 		<%= form_for(current_user.messages.new, :url => {:controller => :messages, :action => 'create'}) do |f| %>
          <%= f.text_area :body,  class: "left", placeholder: "Type message here..."  %>
          <%= f.submit "Send", :class => "btn left" %>
          <% end %>

	</div>
</div>